#include "push_swap.h"

t_list *initialisation()
{
    t_list  *stackA = malloc(sizeof(t_list));


    if (stackA == NULL)
    {
        return(NULL));
    }

    element->nombre = 0;
    element->suivant = NULL;
    liste->premier = element;

    return liste;
}



